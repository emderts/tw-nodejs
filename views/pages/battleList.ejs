<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>ThirdWorld - <%= title %></title>
    <link href="https://fonts.googleapis.com/css?family=Nanum+Gothic" rel="stylesheet">
    <style>
      body {font-family: "Nanum Gothic", sans-serif; }
      div {width: 450px; max-width: 100%; margin: 0 auto; text-align: center; }
      .logDate {font-size: 12px; }
      .charItem button {width: 450px; max-width: 100%; background-color: #ffffea; margin-top: 5px; }
      .charItem button:hover {background-color: #ffffcc; }
      .backLink {text-align: center; width: 40%; margin: 10px auto; overflow: hidden; }
    </style>
</head>
<body>
  <% for (val of list) { %>
    <div class="charItem">
      <form action="<%= formAction %>" method="post">
        <button type="submit"><%= val.name %></button>
        <input type="hidden" name="charUid" value="<%= val.uid %>">
      </form>
      <% if (val.battleCnt != undefined) { %>
        <div class="logDate">전체 <%= val.battleCnt %>전 <%= val.winCnt %>승 (<%= val.winRate %>%), 최근 <%= val.recentBattleCnt %>전 <%= val.recentWinCnt %>승 (<%= val.recentWinRate %>%), 당신에게 <%= val.vsBattleCnt %>전 <%= val.vsWinCnt %>승 (<%= val.vsWinRate %>%)</div>
      <% } %>
    </div>
  <% } %>
  <div class="backLink">
    <a href="/">돌아가기</a>
  </div>
</body>
</html> 
